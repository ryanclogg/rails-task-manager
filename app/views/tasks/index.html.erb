<h1>View all your tasks here:</h1>

<ul>
  <% @tasks.each do |task| %>
  <li>
    <% if task.completed == true %>
    🦄
    <% else %>
    🤥
    <% end %>
    <%= link_to task.title, task_path(task) %>
    <%= link_to '🗑', delete_task_path(task), method: :delete %>
  </li>
  <% end %>
</ul>

<%= link_to "Create New Tasks", new_task_path %>
